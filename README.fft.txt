Code Name: fft.py

Input: time series, time vector

Usage:  fft.npfft(x,t); where x is the time series of amplitude, and t is the time vector

output: Normalised amplitude of FFT and Frequency

Plot: Try  below 

# fft.npfft(x, t)
# plt.ylabel("Amplitude")
# plt.xlabel("Frequency [Hz]")
# plt.bar(f[:N // 2], np.abs(fft)[:N // 2] * 1 / N, width=1.5)  # 1 / N is a normalization factor
# plt.show()

