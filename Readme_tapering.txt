tepering_window.py create the tapered time series data using different  tapering methods. 
default = Hanning window

Example:
        tepering_window(time_series)
        tepering_window(time_series,"Tukey")

